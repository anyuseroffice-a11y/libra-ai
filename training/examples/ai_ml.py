"""AI/ML examples."""

ML_TRAIN = [('What is a transformer?', 'Architecture using self-attention to process sequences. Encoder-decoder with multi-head attention. Replaced RNNs for most NLP tasks. Foundation of modern LLMs.'), ('Explain attention mechanism.', ' Computes relevance scores between query and keys. Scaled dot-product: softmax(QK^T/sqrt(d))V. Allows model to focus on relevant parts of input.'), ('What is tokenization?', 'Converting text to numerical tokens. BPE merges frequent character pairs. WordPiece for BERT. SentencePiece for multilingual. Determines vocabulary size and input representation.'), ('Explain embeddings.', 'Dense vector representations of discrete items. Word2Vec, GloVe for words. Sentence transformers for text. Capture semantic relationships in continuous space.'), ('What is RAG?', 'Retrieval-Augmented Generation: retrieve relevant documents, inject into LLM context. Combines knowledge base with generative model. Reduces hallucination, enables current knowledge.'), ('Explain LoRA fine-tuning.', 'Low-Rank Adaptation: freeze base model, train low-rank decomposition matrices. Reduces trainable parameters by 100-1000x. Applied to attention layers.'), ('What is QLoRA?', 'Quantized LoRA: combine 4-bit quantization with LoRA fine-tuning. Enables fine-tuning 65B models on single 48GB GPU. NF4 quantization.'), ('Explain transformer attention math.', 'Attention(Q,K,V) = softmax(QK^T/sqrt(d_k))V. Multi-head: concat multiple attention heads. Each head learns different relationship patterns.'), ('What is temperature in LLM sampling?', 'Controls randomness in token selection. T=1: standard sampling. T<1: more deterministic, peaked distribution. T>1: more random, flatter distribution.'), ('Explain top-p sampling.', 'Nucleus sampling: sample from smallest set of tokens whose cumulative probability >= p. Dynamically adjusts vocabulary size. Balances coherence and diversity.'), ('What is context window?', 'Maximum number of tokens an LLM can process. Includes input + output. GPT-4: 128K. Limited by attention mechanism quadratic cost.'), ('Explain overfitting.', 'Model memorizes training data instead of learning patterns. Signs: low train loss, high val loss. Fixes: more data, regularization, dropout, early stopping.'), ('What is a neural network?', 'Layers of connected neurons with learnable weights. Forward pass computes output. Backpropagation computes gradients. Loss function measures error.'), ('Explain gradient descent.', 'Optimization by updating parameters in negative gradient direction. Learning rate controls step size. Variants: SGD, Adam, AdaGrad. Converges to local minimum.'), ('What is a CNN?', 'Convolutional Neural Network: applies learnable filters to detect patterns. Pooling reduces spatial dimensions. Effective for images, also text classification.'), ('Explain RNN and LSTM.', 'RNN processes sequences with hidden state. LSTM adds gates (forget, input, output) to handle long-range dependencies. GRU simplifies LSTM with fewer gates.'), ('What is batch normalization?', 'Normalize layer inputs to zero mean, unit variance. Speeds training, allows higher learning rates. Applied before activation function.'), ('Explain dropout.', 'Randomly zero neurons during training. Prevents co-adaptation, acts as ensemble. Usually 20-50% rate. Disabled during inference.'), ('What is Adam optimizer?', 'Adaptive learning rate method combining momentum and RMSProp. Maintains per-parameter learning rates. Good default for most problems.'), ('Explain backpropagation.', 'Compute gradients of loss w.r.t. parameters using chain rule. Forward pass computes activations. Backward pass propagates error gradients. O(n) per layer.'), ('What is fine-tuning?', 'Continue training pre-trained model on specific task. Lower learning rate than pre-training. Freeze early layers, train later layers. Adapt general model to domain.'), ('Explain token limits in LLMs.', 'LLMs have fixed context window. Input tokens + output tokens must fit. Exceeding causes truncation. Strategies: chunking, sliding window, summarization.'), ('What is prompt engineering?', 'Designing input text to get desired LLM output. Techniques: few-shot examples, chain-of-thought, role prompting, constrained output.'), ('Explain hallucination in LLMs.', 'Model generates plausible but false information. Caused by: training data gaps, pattern matching without understanding. Mitigation: RAG, fact-checking, constrained decoding.'), ('What is model quantization?', 'Reduce precision of model weights (FP16, INT8, INT4). Reduces memory and speeds inference. Trade-off: slight accuracy loss. Enables deployment on edge devices.')]

ML_VAL = [('What is self-attention?', 'Mechanism where each token attends to all other tokens. Computes pairwise relevance. O(n^2) in sequence length. Enables parallel processing of sequences.'), ('Explain BERT.', 'Bidirectional encoder, pre-trained with masked language modeling and next sentence prediction. Good for classification, NER, question answering.'), ('What is GPT architecture?', 'Decoder-only transformer. Auto-regressive: predict next token. Pre-trained on next token prediction. Generates text sequentially.'), ('Explain transfer learning.', 'Use knowledge from one task to improve another. Pre-train on large corpus, fine-tune on small dataset. Foundation of modern NLP.'), ('What is a language model?', 'Probability distribution over token sequences. P(w1,w2,...,wn). Can be autoregressive (GPT) or masked (BERT). Trained to predict next/masked tokens.'), ('Explain multi-head attention.', 'Multiple attention heads in parallel, each learning different relationships. Concatenate outputs, project with linear layer. Allows attending to different positions simultaneously.'), ('What is positional encoding?', 'Inject sequence order information since transformers lack recurrence. Sinusoidal (fixed) or learned embeddings. Added to token embeddings before attention.'), ('Explain encoder-decoder architecture.', 'Encoder processes input into representation. Decoder generates output conditioned on encoder. Used in translation, summarization. Cross-attention connects them.'), ('What is beam search?', 'Keep top-k (beam width) most probable sequences at each step. More thorough than greedy, less random than sampling. Used in translation and summarization.'), ('Explain knowledge distillation.', 'Train smaller student model to mimic larger teacher. Soft targets from teacher provide more information than hard labels. Reduces model size with minimal accuracy loss.'), ('What is a data pipeline for ML?', 'Sequence: data collection, cleaning, feature engineering, splitting, training, evaluation. Needs versioning, reproducibility, monitoring.'), ('Explain ROC-AUC.', 'Receiver Operating Characteristic curve plots TPR vs FPR at various thresholds. AUC = area under curve. 0.5 = random, 1.0 = perfect. Threshold-independent metric.'), ('What is cross-validation?', 'Split data into k folds. Train on k-1, validate on 1. Rotate. Average metrics. More reliable estimate of generalization performance.'), ('Explain feature engineering.', 'Transform raw data into model inputs. Categorical encoding, scaling, interaction terms, text vectorization. Domain knowledge improves model performance.'), ('What is a confusion matrix?', 'Table of TP, FP, TN, FN. From this: accuracy, precision, recall, F1. Shows types of errors model makes.'), ('Explain precision vs recall.', 'Precision = TP/(TP+FP) - of predicted positives, how many correct. Recall = TP/(TP+FN) - of actual positives, how many found. Trade-off between them.'), ('What is regularization?', 'Techniques to prevent overfitting: L1/L2 weight penalty, dropout, early stopping, data augmentation. Adds constraint to model complexity.'), ('Explain batch vs stochastic gradient descent.', 'Batch GD uses all data per update - stable but slow. SGD uses one sample - noisy but fast. Mini-batch is compromise. Most common in practice.'), ('What is a loss function?', 'Measures prediction error. MSE for regression, cross-entropy for classification, contrastive loss for similarity. Minimized during training.'), ('Explain learning rate scheduling.', 'Adjust learning rate during training. Start high for fast convergence, decay for fine-tuning. Cosine annealing, warm-up, step decay.'), ('What is an embedding layer?', 'Lookup table mapping discrete tokens to dense vectors. Learnable during training. Output dimension is embedding size. Foundation of NLP models.'), ('Explain chain-of-thought prompting.', 'Ask model to show reasoning steps before final answer. Improves accuracy on multi-step problems. Mimics human problem-solving process.'), ('What is few-shot learning?', 'Provide a few examples in the prompt to guide model behavior. No fine-tuning needed. Leverages in-context learning ability of large models.'), ('Explain model evaluation metrics.', 'Classification: accuracy, precision, recall, F1, AUC. Regression: MSE, MAE, R-squared. NLP: BLEU, ROUGE, perplexity. Choose metric aligned with goal.'), ('What is neural network initialization?', 'Random weight initialization breaks symmetry. Xavier/He initialization maintains variance across layers. Poor initialization causes vanishing/exploding gradients.')]
