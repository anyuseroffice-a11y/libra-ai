"""System design examples."""

SYS_TRAIN = [('Design a URL shortener.', 'Hash or base62 encode a counter. Store mapping in Redis for speed, MySQL for persistence. Use 301 redirects. Consider collision handling and custom slugs.'), ('Design a chat system.', 'WebSocket for real-time messages. Store messages in database with conversation ID. Use message queue for fan-out. Presence via heartbeat. Read receipts with ACK.'), ('How would you design rate limiting?', 'Token bucket or sliding window. Redis for distributed counters. Return 429 with Retry-After header. Different limits per endpoint and user tier.'), ('Design a notification system.', 'Event-driven: producers emit events, notification service consumes them. Support push, email, SMS via separate workers. Use priority queue for urgent notifications.'), ('Design a file storage service.', 'Object storage with content-addressable hashing. Store metadata in MySQL, files in S3-compatible storage. Chunk large files. Pre-signed URLs for access.'), ('Design a search system.', 'Inverted index for full-text search. Elasticsearch or custom implementation. Rank by relevance (TF-IDF or BM25). Support autocomplete with trie.'), ('How would you design an e-commerce backend?', 'Product catalog in MySQL with Redis cache. Cart in Redis session. Orders with ACID transactions. Payment via Stripe/PayPal integration. Inventory with optimistic locking.'), ('Design an authentication system.', 'JWT access tokens with short expiry, refresh token rotation. Password hashing with bcrypt. OAuth2 for social login. Rate limit login attempts.'), ('Design a payment processing architecture.', 'Idempotent payment requests with unique keys. Webhook for async confirmation. Two-phase: authorize then capture. Reconciliation job. PCI compliance.'), ('How would you implement caching?', 'Cache-aside pattern: check cache first, fallback to DB. Use Redis with TTL. Cache invalidation on write. Consider stampede with locks.'), ('Design a URL shortener at scale.', 'Base62 encoding of auto-increment ID. Bloom filter for existence check. Redis for hot mappings. MySQL for persistence. Analytics via write-behind.'), ('Design a real-time leaderboard.', 'Redis sorted set for O(log N) rank updates. Shards by game ID. Periodic snapshot to MySQL for persistence. Handle ties with secondary sort.'), ('How would you design a news feed?', 'Pull model: fan-out on read, query followees posts. Push model: fan-out on write to followers inbox. Hybrid: push for users with few followers, pull for celebrities.'), ('Design a webhook delivery system.', 'Queue-based with retry. Exponential backoff. Dead letter queue for permanent failures. Signature verification. Delivery logs for debugging.'), ('How would you design a task scheduler?', 'Job queue with Redis or RabbitMQ. Workers poll for jobs. Support cron expressions. Priority levels. Dead letter for failed jobs. Monitoring dashboard.'), ('Design a content delivery strategy.', 'CDN for static assets with cache headers. Origin server for dynamic content. Edge caching with invalidation. Consider cache-busting with versioned filenames.'), ('How would you design a logging system?', 'Structured logging to stdout. Log collector (Fluentd/Logstash). Centralized storage (Elasticsearch). Alerting on patterns. Retention policies.'), ('Design a notification preference system.', 'User preferences stored per notification type and channel. Check preferences before sending. Support do-not-disturb windows. Audit trail.'), ('How would you design a multi-tenant system?', 'Shared database with tenant_id column. Row-level security. Separate schemas for isolation. Resource quotas per tenant. Connection pooling.'), ('Design a real-time collaboration tool.', 'Operational Transformation or CRDT for concurrent edits. WebSocket for real-time sync. Version history with conflict resolution. Presence indicators.')]

SYS_VAL = [('Design a simple cache.', 'LRU cache with O(1) lookup and eviction. Use HashMap + doubly linked list. Set TTL for expiry. Consider size limits.'), ('How would you design a URL validator?', 'Regex for format, DNS lookup for domain existence, HTTP check for accessibility. Handle international domains with IDN encoding.'), ('Design a rate limiter for an API.', 'Token bucket algorithm. Redis for distributed state. Key by API key or IP. Return remaining quota in headers. Support different limits per endpoint.'), ('How would you design a feature flag system?', 'Toggle stored in database, cached in Redis. Evaluation at application level. Gradual rollout by percentage. A/B testing support.'), ('Design a simple search autocomplete.', 'Trie with frequency counts. Update counts on search. Return top-k suggestions. Cache popular queries. Consider typo tolerance.'), ('How would you design audit logging?', 'Append-only table recording who did what and when. Store old and new values for mutations. Queryable by actor, time range, and action type.'), ('Design a session management system.', 'Server-side sessions stored in Redis. Random session IDs. Automatic expiry. Regeneration on privilege change. Secure cookie flags.'), ('How would you design a configuration management system?', 'Hierarchical config with environment overrides. Version controlled. Hot reload via file watcher or API. Validate config on startup.'), ('Design a simple message queue.', 'Circular buffer with producers and consumers. Support multiple consumer groups. Persistent storage for durability. Dead letter for failed messages.'), ('How would you design a health check endpoint?', 'Check database connectivity, Redis, external services. Return 200 if healthy, 503 if not. Include dependency status in response body.'), ('Design a database migration system.', 'Versioned SQL files applied in order. Track applied migrations in a meta table. Support up and down migrations. Dry-run mode.'), ('How would you design a file upload system?', 'Stream to temp file, validate type and size, move to permanent storage. Generate unique filename. Return URL. Support chunked uploads for large files.'), ('Design a simple CDN.', 'Reverse proxy with caching. Cache static assets by URL. Respect Cache-Control headers. Purge by tag or URL pattern. Origin fallback.'), ('How would you design a task queue?', 'Redis list with BRPOPLPUSH for reliable processing. Worker pool consuming jobs. Retry with exponential backoff. Priority queue support.'), ('Design a simple load balancer.', 'Round-robin or least-connections algorithm. Health checks on backends. Session affinity option. Weighted distribution. Connection draining on shutdown.'), ('How would you design API versioning?', 'URL path versioning (/v1/, /v2/). Deprecation headers. Backward-compatible changes preferred. Documentation for breaking changes.'), ('Design a webhook system.', 'Outgoing HTTP calls with retry. Signature header for verification. Event filtering per subscriber. Delivery logs with response codes.'), ('How would you design a data pipeline?', 'ETL: Extract from sources, Transform in processing layer, Load to destination. Use message queue for decoupling. Monitor lag and failures.'), ('Design a simple distributed lock.', 'Redis SET NX EX for acquire, Lua script for release. Fencing token to prevent stale locks. Consider Redlock for multi-node.'), ('How would you design an API gateway?', 'Single entry point for all APIs. Handle auth, rate limiting, request routing. Transform responses. Log all requests. Support WebSocket upgrade.')]
