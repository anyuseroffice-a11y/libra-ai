"""Algorithm and data structure examples."""

ALGO_TRAIN = [('Implement binary search in Python.', 'Time: O(log n), Space: O(1). Works on sorted arrays. Compare mid with target, eliminate half each step.'), ('Time complexity of binary search?', 'O(log n). Each comparison halves the search space.'), ('Explain BFS vs DFS.', 'BFS uses a queue, explores level by level, finds shortest paths in unweighted graphs. DFS uses stack/recursion, goes deep first, good for topological sort and cycle detection.'), ('What is dynamic programming?', 'Solves overlapping subproblems by storing results. Example: Fibonacci with memo is O(n) vs O(2^n) naive.'), ('Stack vs Queue?', 'Stack is LIFO (push/pop same end). Queue is FIFO (enqueue back, dequeue front).'), ('What is a heap?', 'Complete binary tree where parent dominates children. Used for priority queues. Insert/extract O(log n).'), ('Explain Dijkstra algorithm.', 'Finds shortest paths from source in weighted graph with non-negative edges. Uses priority queue. O((V+E) log V).'), ('BST vs hash table?', 'BST: sorted data, range queries O(log n). Hash: O(1) average lookup, no ordering.'), ('Time complexity of quicksort?', 'Average O(n log n), worst O(n^2) with bad pivots. Space O(log n) for recursion stack.'), ('What is a trie?', 'Prefix tree storing strings character by character. O(m) insert/search where m is key length. Used for autocomplete and spell checking.'), ('Explain union-find.', 'Tracks disjoint sets. Find and union nearly O(1) amortized with path compression and union by rank.'), ('Implement BFS on a graph.', 'O(V+E) time. Use queue, track visited, process level by level.'), ('Topological sorting?', 'Orders DAG vertices so edge (u,v) means u before v. DFS post-order reversed or Kahn algorithm with in-degree tracking.'), ('Two pointers technique?', 'Two indices moving through data. Used for: two sum sorted array, palindrome check, finding middle of linked list.'), ('Kruskal vs Prim for MST?', 'Kruskal: sort edges, add non-cycle edges using union-find, O(E log E). Prim: grow tree from vertex using heap, O(E log V).'), ('What is amortized analysis?', 'Averages cost over a sequence. Dynamic array insert is O(1) amortized despite occasional O(n) resize.'), ('Linked list vs array?', 'Array: O(1) random access, O(n) insert/delete. Linked list: O(1) insert at head, O(n) search, no random access.'), ('Explain sliding window.', 'Maintains window over data. O(n) for problems like max sum subarray, longest substring without repeats.'), ('What is a priority queue?', 'Serves highest-priority element first. Binary heap: insert O(log n), extract O(log n), peek O(1).'), ('Implement a stack.', 'Use list: push = append, pop = pop(). Both O(1) amortized.'), ('Explain merge sort.', 'Divide array in half, recursively sort halves, merge sorted halves. O(n log n) always, O(n) space, stable.'), ('What is a graph?', 'Vertices connected by edges. Directed vs undirected. Weighted vs unweighted. Represented by adjacency list or matrix.'), ('Implement DFS.', 'O(V+E) time. Use recursion or explicit stack, track visited.'), ('What is a hash collision?', 'Two keys map to same hash bucket. Resolved by chaining (linked list at bucket) or open addressing (probe next slot).'), ('Explain Bellman-Ford algorithm.', 'Shortest paths from source, handles negative edges. O(VE). Can detect negative cycles.')]

ALGO_VAL = [('What is Big O notation?', 'Upper bound on growth rate. Describes worst-case time/space as input grows. O(1) < O(log n) < O(n) < O(n log n) < O(n^2).'), ('Implement quicksort.', 'Pick pivot, partition array into elements less/greater than pivot, recurse on both halves. Average O(n log n).'), ('What is a hash table?', 'Maps keys to values via hash function. Average O(1) lookup/insert. Worst O(n) with many collisions.'), ('Explain binary search tree.', 'Each node has left < parent < right. Search/insert average O(log n), worst O(n) if unbalanced.'), ('What is graph coloring?', 'Assign colors to vertices so no adjacent vertices share same color. Chromatic number is minimum colors needed.'), ('Explain knapsack problem.', 'Given items with weights and values, maximize value within weight limit. 0/1 knapsack is O(nW) with DP. Fractional is greedy O(n log n).'), ('What is memoization?', 'Cache results of expensive function calls. Trade space for time. Top-down DP approach.'), ('Explain Bellman-Ford.', 'Shortest paths from source with negative edges. O(VE) time. Detects negative cycles.'), ('What is a red-black tree?', 'Self-balancing BST with O(log n) operations. Nodes colored red/black with balancing rules to maintain height log n.'), ('Explain Floyd-Warshall.', 'All-pairs shortest paths in O(V^3). Works with negative edges but not negative cycles.'), ('What is backtracking?', 'Explore all solutions, abandon invalid ones early. Used for N-queens, Sudoku, permutations. Exponential worst case.'), ('Explain greedy algorithms.', 'Make locally optimal choices at each step. Works for activity selection, Huffman coding. Not always globally optimal.'), ('What is divide and conquer?', 'Split problem into subproblems, solve recursively, combine results. Examples: merge sort, quicksort, binary search.'), ('What is a bloom filter?', 'Probabilistic set: O(1) insert/lookup, may have false positives, no false negatives. Space efficient.'), ('BFS shortest path properties?', 'In unweighted graph, BFS from source gives shortest paths to all reachable vertices. O(V+E) time.'), ('What is the Master theorem?', 'Solves recurrences T(n) = aT(n/b) + f(n). Compare f(n) to n^(log_b(a)) to determine complexity class.'), ('Explain dynamic arrays.', 'Resizable arrays like Python list. Amortized O(1) append, double capacity when full. O(n) worst-case resize.'), ('What is a skip list?', 'Layered linked list for O(log n) search. Probabilistic balancing alternative to balanced BSTs.'), ('Explain stable matching.', 'Gale-Shapley algorithm produces stable matching between two sets in O(n^2). No pair prefers someone else over their match.'), ('What is cycle detection?', 'Floyd tortoise and hare algorithm detects cycle in O(n) time, O(1) space. Also: topological sort for directed graphs.')]
